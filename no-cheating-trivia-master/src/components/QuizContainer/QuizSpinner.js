import { Spinner } from "react-bootstrap";

export const spinner =
  <Spinner className="d-flex mx-auto mt-5" animation="border" role="status" variant="primary">
    <span className="sr-only">Loading...</span>
  </Spinner>